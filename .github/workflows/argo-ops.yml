name: CI for argo-demo-deploy/gitops/argo-demo-app-ops

on:
  push:
    paths:
      - 'argo-demo-deploy/gitops/argo-demo-app-ops/**'
  pull_request:
    paths:
      - 'argo-demo-deploy/gitops/argo-demo-app-ops/**'

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Triggered
        run: echo "Changes detected in argo-demo-deploy/gitops/argo-demo-app-ops"
